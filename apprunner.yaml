version: 1.0
runtime: python311
build:
  commands:
    build:
      - python -m pip install -r requirements.txt
      - python manage.py collectstatic --noinput
run:
  command: sh startup.sh
  network:
    port: 8080